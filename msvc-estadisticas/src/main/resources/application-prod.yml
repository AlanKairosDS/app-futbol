server:
  port: 8001
  servlet:
    context-path: /msvc-estadisticas

spring:
  application:
    name: msvc-estadisticas
  autoconfigure:
    exclude: org.springframework.boot.autoconfigure.jdbc.DataSourceAutoConfiguration
  data:
    mongodb:
      database: app_fubol
      host: localhost
      port: 27017
